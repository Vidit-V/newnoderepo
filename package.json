{
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "netlify-cli": "^17.22.1",
    "netlify-lambda": "^2.0.16",
    "serverless-http": "^3.2.0"
  },
  "name": "newnetlifynodejs",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "NODE_ENV=development node functions/api.js",
    "start:prod": "NODE_ENV=production node functions/api.js",
    "build": "echo 'Build steps can be placed here'",
    "deploy:dev": "netlify deploy --site ${NETLIFY_SITE_ID} --dir=dist",
    "deploy:prod": "netlify deploy --site ${NETLIFY_SITE_ID} --dir=dist --prod"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
